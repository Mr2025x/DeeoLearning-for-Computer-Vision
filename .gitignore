# 1. 忽略当前文件夹下的所有内容（一刀切）
*

# 2. 【关键修复】：放行所有的文件夹！
# 只有放行了文件夹，Git 才能走进去寻找你下面指定的 .py 和 .md 文件
!*/

# 3. 【位置调整】：重新忽略 env 虚拟环境文件夹
# 这一步必须放在 !*/ 之后，否则 env 文件夹会被上面的规则放行
env/

# 4. 保留 .gitignore 文件自身
!.gitignore

# 5. 放行你需要的特定文件
!*.py
!*.ipynb
!*.md